# .env.example
# Copy this file to .env and fill in your actual values
# NEVER commit your .env file to version control!

# ============================================
# CLIENT-SIDE (exposed in browser bundle)
# ============================================
# These values are safe to expose publicly
# The VITE_ prefix means they will be included in the client-side JavaScript

# Supabase Project URL (public)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (public, limited permissions)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Netlify Functions URL (public)
# Production: https://your-app.netlify.app/.netlify/functions
# Local dev: http://localhost:8888/.netlify/functions
VITE_NETLIFY_FUNCTIONS_URL=https://your-app.netlify.app/.netlify/functions

# ============================================
# SERVER-SIDE (never exposed to client)
# ============================================
# These values must be kept secret!
# DO NOT use VITE_ prefix for these - they should only exist on the server

# OpenAI API Key (for Whisper transcription and photo analysis)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (for Claude AI analysis and parsing)
# Get yours at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# AssemblyAI API Key (for alternative transcription service with speaker diarization)
# Get yours at: https://www.assemblyai.com/app
ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here

# Google API Key (for Gemini photo enhancement)
# Get yours at: https://console.cloud.google.com/apis/credentials
GOOGLE_API_KEY=your_google_api_key_here

# Supabase Service Role Key (full database access - KEEP SECRET!)
# Get this from: Supabase Dashboard → Project Settings → API → service_role key
# WARNING: This key bypasses Row Level Security - only use in serverless functions
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Resend API Key (for sending emails)
# Get yours at: https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here

# ============================================
# WEB PUSH NOTIFICATIONS (VAPID)
# ============================================
# Generate keys with: npx web-push generate-vapid-keys

# VAPID Public Key (client-side, safe to expose)
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key_here

# VAPID Private Key (server-side only - KEEP SECRET!)
VAPID_PRIVATE_KEY=your_vapid_private_key_here

# VAPID Subject (typically mailto: or https:// URL)
VAPID_SUBJECT=mailto:notifications@discountfence.com

# ============================================
# BULK UPLOAD CONFIGURATION (optional)
# ============================================
# Default User ID for bulk photo uploads (UUID format)
# Get from: Supabase Dashboard → Authentication → Users
DEFAULT_USER_ID=your_user_uuid_here

# ============================================
# SECURITY NOTES
# ============================================
# ⚠️ CRITICAL SECURITY RULES:
#
# 1. VITE_ prefix = Client-side (Public)
#    - Only use for values that are safe to expose publicly
#    - Examples: Supabase URL, Anon Key, API endpoints
#    - These get embedded in your JavaScript bundle
#
# 2. NO prefix = Server-side (Secret)
#    - Use for API keys, service role keys, secrets
#    - These should ONLY be used in Netlify Functions
#    - NEVER import these in client-side code
#
# 3. For production deployment:
#    - Add ALL variables to Netlify Dashboard
#    - Site settings → Environment variables
#    - Use the exact same names as shown here
#
# 4. Verify .env is in .gitignore before committing!
#
# ============================================
# QUICKBOOKS ONLINE (QBO) INTEGRATION
# ============================================
# OAuth credentials for QuickBooks Online API
# Get these from: https://developer.intuit.com/app/developer/dashboard

# QBO Client ID (public identifier)
QBO_CLIENT_ID=your_qbo_client_id_here

# QBO Client Secret (KEEP SECRET!)
QBO_CLIENT_SECRET=your_qbo_client_secret_here

# QBO Redirect URI (must match what's configured in Intuit Developer Portal)
# Production: https://discount-fence-hub.netlify.app/.netlify/functions/qbo-callback
# Local dev: http://localhost:8888/.netlify/functions/qbo-callback
QBO_REDIRECT_URI=https://discount-fence-hub.netlify.app/.netlify/functions/qbo-callback

# QBO Environment: 'sandbox' for testing, 'production' for live
QBO_ENVIRONMENT=production

# ============================================
# JOBBER INTEGRATION
# ============================================
# OAuth credentials for Jobber API (Field Service Management)
# Get these from: https://developer.getjobber.com/
# Supports multiple accounts: residential, builders, commercial

# Jobber Client ID (public identifier)
JOBBER_CLIENT_ID=your_jobber_client_id_here

# Jobber Client Secret (KEEP SECRET!)
JOBBER_CLIENT_SECRET=your_jobber_client_secret_here

# Jobber Redirect URI (must match what's configured in Jobber Developer Center)
# Production: https://discount-fence-hub.netlify.app/.netlify/functions/jobber-callback
# Local dev: http://localhost:8888/.netlify/functions/jobber-callback
JOBBER_REDIRECT_URI=https://discount-fence-hub.netlify.app/.netlify/functions/jobber-callback

# Jobber API Version (check https://developer.getjobber.com/docs/using_jobbers_api/api_versioning/)
JOBBER_API_VERSION=2025-01-20

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual API keys in the .env file
# 3. Run: git status (verify .env is not tracked)
# 4. For local development: npm run dev or netlify dev
# 5. For production: Add variables to Netlify Dashboard
